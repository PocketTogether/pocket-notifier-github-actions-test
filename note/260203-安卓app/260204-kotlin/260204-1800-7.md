按 **7 号口令执行**。  
下面是 **完整、可直接使用、最小依赖、最小 APK、R8 启用、可 fork 的 Gradle 配置**。

我保持了你一贯的工程风格：**显式、机制化、可维护、可扩展、可复用、最小依赖**。

---

# 📄 **Project 级 `build.gradle.kts`**

路径：

```
build.gradle.kts
```

```kotlin
// 顶级 Gradle 配置（最小化，无多余插件）

buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
```

---

# 📄 **Project 级 `settings.gradle.kts`**

（此文件在口令 1 已生成，这里不重复，只补充必要内容）

```kotlin
rootProject.name = "PocketNotifier"
include(":app")
```

---

# 📄 **App 级 `app/build.gradle.kts`**

路径：

```
app/build.gradle.kts
```

这是 **最小依赖 + R8 + minifyEnabled + Kotlin + OkHttp + Coroutines** 的完整配置。

```kotlin
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {

    namespace = "com.pocket.notifier" // TODO: 可修改应用 ID
    compileSdk = 34

    defaultConfig {
        applicationId = "com.pocket.notifier" // TODO: 可修改应用 ID
        minSdk = 24
        targetSdk = 34

        versionCode = 1 // TODO: 可修改
        versionName = "1.0.0" // TODO: 可修改

        // 启用 MultiDex（如果未来依赖变多）
        multiDexEnabled = true
    }

    buildTypes {
        release {
            isMinifyEnabled = true      // 启用 R8 压缩
            isShrinkResources = true    // 移除未使用资源
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }

        debug {
            isMinifyEnabled = false
            isShrinkResources = false
        }
    }

    // 使用 ViewBinding
    buildFeatures {
        viewBinding = true
    }

    // Kotlin JVM 目标
    kotlinOptions {
        jvmTarget = "17"
    }

    // 兼容旧设备
    packaging {
        resources.excludes += "META-INF/*"
    }
}

dependencies {

    // Kotlin 标准库（最小依赖）
    implementation("org.jetbrains.kotlin:kotlin-stdlib:1.9.22")

    // 协程（最小依赖）
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")

    // OkHttp（最小依赖）
    implementation("com.squareup.okhttp3:okhttp:4.12.0")

    // AndroidX 基础库（最小依赖）
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")

    // ViewBinding 不需要额外依赖

    // 通知需要的依赖（最小）
    implementation("com.google.android.material:material:1.11.0")
}
```

---

# 📄 **proguard-rules.pro（自动引用）**

路径：

```
app/proguard-rules.pro
```

内容极简（OkHttp + 协程无需额外规则）：

```pro
# 保留 OkHttp 日志类（如果未来启用）
-dontwarn okhttp3.**

# 保留 Kotlin 协程（一般不需要，但更安全）
-dontwarn kotlinx.coroutines.**
```

---

# 🎯 **APK 最小化策略总结**

你现在的配置已经做到：

### ✔ 启用 R8（代码压缩）

`isMinifyEnabled = true`

### ✔ 启用资源压缩

`isShrinkResources = true`

### ✔ 最小依赖

- Kotlin stdlib
- Coroutines
- OkHttp
- AndroidX Core + AppCompat
- Material（通知需要）

### ✔ 无多余插件

没有 kapt、没有 Compose、没有 Hilt、没有 Room  
→ **APK 最小化**

### ✔ JVM 17（现代 Android 推荐）

更快、更小、更稳定。

---

# 🎉 7 号口令执行完毕

如果你准备继续下一步，可以直接说：

> 按 8 号口令执行

我将为你生成 **GitHub Actions 自动打包脚本（android.yml）**，完全可 fork、可复用、可自动构建 release APK。