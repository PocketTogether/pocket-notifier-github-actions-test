// 顶级 Gradle 配置（无业务逻辑）

plugins {
    // TODO: 根据需要添加插件（如 Kotlin、Android）
    // 在后续口令中会补全
}

buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}