```
adb logcat | findstr "pocket_notifier"

adb logcat | findstr /C:"02-05 17:3"

adb logcat | findstr "pocket_notifier 02-05 17"

adb logcat | findstr "pocket_notifier" | findstr "02-05" | findstr "17"

adb logcat | findstr "pocket_notifier" | findstr /C:"02-05 17:4"

adb logcat | findstr /i "AndroidRuntime" | findstr /C:"02-05 17:4"

```


目前安装成功、运行成功、通知成功
```
请求失败
```

TODO
```
通知默认重要程度完善

应用id标准完善

界面完善，
明暗主题完善
界面中的主图片，点击后用浏览器打开配置中配置的网址
主图片下方有一个设置按钮，点击后跳转到应用设置，以便于配置通知重要程度、声音、振动、是否弹出之类的

应用图标修改，缩小一点
请求处理，处理数据，根据数据内容通知
sse支持，pb实时订阅
```