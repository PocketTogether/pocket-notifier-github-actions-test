```
这是我现在的项目（如上传的文件所示）

我想实现这里的图片（应用主页的主图片）的这一块
app\src\main\res\layout\activity_main.xml
    <!-- 居中显示状态图片 -->
    <ImageView。。。。。。。
点击这个时，能用浏览器打开指定网址如 https://uika.top
这个指定网址记得要添加到 app\src\main\java\com\pocket\notifier\config\Config.kt

我还想实现一个
在主图片下方，还有一个设置图标，点击其能跳转到手机的应用管理页面（手机上本应用的管理页面）
这样就能用户便于配置通知重要程度、声音、振动、是否弹出之类的
居中情况注意一下，我想要的时，将主图片和设置图标视为一个整体，在页面中居中
设置图标这一块，是有现成的图标能用吗（不新添加依赖的情况下），如果没有的话就给我写一个drawable/xxxx帮我命名.xml吧
图标需要是可设置颜色的，颜色就为应用前景色（文字色）

帮我写，记得要给我完整的文件路径
```


```
        binding.statusImage.setOnClickListener {
            val intent = Intent(Intent.ACTION_VIEW).apply {
                data = android.net.Uri.parse(Config.CLICK_URL)
            }
            startActivity(intent)
        }

        binding.settingsIcon.setOnClickListener {
            val intent = Intent(android.provider.Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
                data = android.net.Uri.fromParts("package", packageName, null)
            }
            startActivity(intent)
        }

这个确认对吗，确认能用浏览器打开链接吗，确认能打开本应用的设置页面吗
packageName是什么难道不该用 应用id 吗，因为开源项目，别的项目可能和自己的项目的包名与项目目录路径一样啊
```



